<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>64bitd - Perfil de <%= user.nome %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> 
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <style>
        .profile-header {
            background-image: url('/img/default-banner.jpg');
            background-size: cover;
            background-position: center;
            height: 250px;
        }
        .profile-avatar-large {
            width: 150px;
            height: 150px;
            object-fit: cover;
            border-radius: 50%;
        }
        .edit-btn {
            background-color: #343a40;
            color: #fff;
            padding: 8px 15px;
            border-radius: 6px;
            transition: background-color 0.2s;
        }
        .edit-btn:hover {
            background-color: #495057;
        }
        .game-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 20px;
            padding-top: 20px;
        }
        .game-item {
            text-align: center;
            overflow: hidden;
        }
        .game-item img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    
    <aside class="sidebar"></aside>

    <main class="main-content">
        
        <div class="profile-header"></div>

        <div class="container" style="max-width: 1200px;">
            <div class="row">
                <div class="col-md-3 text-center"> 
                    <img src="<%= user.avatar || '/img/user-avatar.jpg' %>" 
                        alt="Avatar do Usuário" 
                        class="profile-avatar-large mt-n5 border border-5 border-darker-bg">
                </div>

                <div class="col-md-9 pt-4">
                    <h1 class="text-white fw-bold mb-1"><%= user.nome %></h1>
                    <p class="text-secondary small mb-3">@<%= user.usuario %></p>
                    
                    <p class="profile-bio text-light mb-4">
                        <%= user.bio || 'Sem descrição.' %>
                    </p>

                    <% if (user.isCurrentUser) { %>
                        <a href="/perfil/editar" class="edit-btn text-decoration-none d-inline-block text-center mb-4">
                            <i class="fas fa-edit me-1"></i> 
                            Editar Perfil
                        </a>
                    <% } %>

                    <div class="profile-stats-container d-flex gap-4 mb-5">
                        <div class="stat-box text-center">
                            <h4 class="text-white mb-0">--</h4>
                            <span class="small text-secondary">Reviews</span>
                        </div>
                        <div class="stat-box text-center">
                            <h4 class="text-white mb-0">--</h4>
                            <span class="small text-secondary">Listas</span>
                        </div>
                    </div>
                </div>
            </div>

            <h3 class="favorites-title mt-5">Jogos Favoritos (<%= user.jogosFavoritos.length %>)</h3>
            
            <div class="game-grid">
                <% user.jogosFavoritos.forEach(jogo => { %>
                    <a href="/jogo/<%= jogo.slug %>" class="game-item text-decoration-none text-light" style="cursor: pointer;">
                        <img src="<%= jogo.img %>" alt="<%= jogo.titulo %>">
                        <p><%= jogo.titulo %></p>
                    </a>
                <% }); %>
                <% if (user.jogosFavoritos.length === 0) { %>
                    <p class="text-secondary mt-3">Nenhum jogo favorito listado.</p>
                <% } %>
            </div>
        </div>

    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/script.js"></script>
</body>
</html>